# 🔍 Проверка и устранение проблем с DNS записями

## ⏱️ ВРЕМЯ РАСПРОСТРАНЕНИЯ DNS

DNS записи распространяются не мгновенно:
- **Минимум:** 15-30 минут
- **Обычно:** 1-2 часа  
- **Максимум:** 24-48 часов
- **В редких случаях:** до 72 часов

**Если прошло меньше 2 часов - подождите!**

---

## ✅ ПРОВЕРКА: ДОБАВЛЕНЫ ЛИ ЗАПИСИ В DNS ЗОНЕ REG.RU?

### 1. Зайдите в панель REG.RU

1. Откройте: https://www.reg.ru/
2. Войдите в аккаунт
3. Перейдите: **"Мои домены"** → **yumivo-support.ru**
4. Откройте: **"DNS-серверы и зоны"** → **"Редактировать зону"** (или **"Управление DNS"**)

### 2. Проверьте, есть ли следующие TXT записи:

#### 📋 СПИСОК ЗАПИСЕЙ, КОТОРЫЕ ДОЛЖНЫ БЫТЬ:

**A) SPF запись:**
- **Имя (Host):** `@` (или пустое, или `.`)
- **Тип:** `TXT`
- **Значение:** `v=spf1 include:_spf.regru.ru ip4:31.31.197.65 ~all`

**B) DMARC запись:**
- **Имя (Host):** `_dmarc` (с подчеркиванием!)
- **Тип:** `TXT`
- **Значение:** `v=DMARC1; p=none; rua=mailto:yumivo@yumivo-support.ru`

**C) DKIM запись (добавляется автоматически REG.RU):**
- **Имя (Host):** `dkim._domainkey` (или `default._domainkey`)
- **Тип:** `TXT`
- **Значение:** Длинная строка, начинающаяся с `v=DKIM1`

---

## ❓ ЧТО ЕСЛИ ЗАПИСЕЙ НЕТ В DNS ЗОНЕ?

### Если SPF или DMARC отсутствуют:

1. **Добавьте их ВРУЧНУЮ** (см. инструкцию выше)
2. **Убедитесь, что:**
   - Имя записано правильно (`@` для SPF, `_dmarc` для DMARC)
   - Тип = `TXT`
   - Значение записано точно (без лишних пробелов)
   - Нажата кнопка **"Сохранить"** или **"Применить"**

### Если DKIM отсутствует:

1. Проверьте, что **DKIM включен** в разделе **"Почта"** → **"Почтовые домены"**
2. Если включен, но записи нет - подождите еще 1-2 часа
3. REG.RU иногда добавляет DKIM с задержкой

---

## 🔍 АЛЬТЕРНАТИВНЫЕ СПОСОБЫ ПРОВЕРКИ

### 1. Проверка через терминал (если доступен dig):

```bash
# Проверка SPF
dig TXT yumivo-support.ru | grep spf

# Проверка DMARC
dig TXT _dmarc.yumivo-support.ru | grep DMARC

# Проверка DKIM
dig TXT dkim._domainkey.yumivo-support.ru | grep DKIM
```

### 2. Онлайн сервисы:

- **SPF:** https://mxtoolbox.com/spf.aspx
- **DMARC:** https://dmarcian.com/dmarc-inspector/
- **DKIM:** https://dkimvalidator.com/
- **Общая проверка DNS:** https://mxtoolbox.com/SuperTool.aspx

### 3. Проверка через сайты REG.RU:

- Обычно есть встроенная проверка DNS в панели управления

---

## ⚠️ ЧАСТЫЕ ОШИБКИ

### Ошибка 1: Запись добавлена, но не сохранилась
- **Решение:** Убедитесь, что нажата кнопка **"Сохранить"** или **"Применить"**

### Ошибка 2: Неправильное имя записи
- **SPF:** Должно быть `@` или пустое (для корня домена)
- **DMARC:** Должно быть `_dmarc` (с подчеркиванием в начале!)

### Ошибка 3: Неправильный тип записи
- **Все записи должны быть типа TXT!**

### Ошибка 4: Лишние пробелы или кавычки в значении
- **Значение должно быть точной копией:** `v=spf1 include:_spf.regru.ru ip4:31.31.197.65 ~all`
- Без лишних пробелов в начале/конце

---

## 🎯 ПЛАН ДЕЙСТВИЙ

### СЕЙЧАС:
1. ✅ Зайдите в панель REG.RU
2. ✅ Откройте DNS зону для yumivo-support.ru
3. ✅ Проверьте, есть ли записи SPF и DMARC
4. ✅ Если нет - добавьте их вручную (см. выше)

### ЧЕРЕЗ 1 ЧАС:
1. ⏰ Проверьте на https://mxtoolbox.com/spf.aspx
2. ⏰ Проверьте на https://dmarcian.com/dmarc-inspector/
3. ⏰ Если записи все еще не найдены - проверьте, правильно ли они добавлены в DNS зоне

### ЧЕРЕЗ 2-4 ЧАСА:
1. ⏰ Повторите проверку
2. ⏰ Если записи появились - отправьте тестовое письмо на Gmail
3. ⏰ Если записи все еще не найдены - обратитесь в поддержку REG.RU

---

## 📞 ЕСЛИ НЕ РАБОТАЕТ ПОСЛЕ 24 ЧАСОВ

Обратитесь в поддержку REG.RU:
- **Чат:** на сайте reg.ru
- **Телефон:** указан на сайте
- **Сообщите:** 
  - "Не могу добавить SPF и DMARC записи для домена yumivo-support.ru"
  - "DNS записи не распространяются после добавления"
  - Приложите скриншоты DNS зоны

---

## ✅ ПРОВЕРКА УСПЕХА

Когда все заработает, вы должны увидеть:

**SPF:**
```
v=spf1 include:_spf.regru.ru ip4:31.31.197.65 ~all
```

**DMARC:**
```
v=DMARC1; p=none; rua=mailto:yumivo@yumivo-support.ru
```

**DKIM:**
```
v=DKIM1; k=rsa; p=... (длинная строка)
```

После этого письма начнут доставляться! 🎉

