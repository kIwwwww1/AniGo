# Настройка фонового изображения профиля

## Обзор улучшений

Код был оптимизирован и улучшен с соблюдением принципов SOLID и best practices.

## Что было улучшено

### 1. 🔐 Безопасность (КРИТИЧНО)
- ✅ **Вынесены S3 креденшалы в переменные окружения** (ранее были в коде)
- ✅ **Добавлена проверка премиум-статуса** для загрузки фонового изображения
- ✅ Валидация размеров и типов файлов

### 2. 🏗️ Архитектура (SOLID)
- ✅ **Создан переиспользуемый `FileWrapper`** в `src/utils/file_wrapper.py`
- ✅ **Создан `ImageValidator`** в `src/utils/image_validator.py`
- ✅ Удален дублированный код
- ✅ Разделение ответственности (Single Responsibility Principle)

### 3. ⚡ Производительность
- ✅ **Добавлены индексы** в модель `UserProfileSettingsModel`:
  - `user_id` (для быстрого поиска по пользователю)
  - `collector_badge_expires_at` (для проверки активных бейджей)
- ✅ Оптимизирована обработка изображений

### 4. 🧹 Чистота кода
- ✅ Удалены неиспользуемые импорты (`PIL`, `io`)
- ✅ Убран дублированный класс `FileWrapper`
- ✅ Добавлены docstrings к утилитам

## Настройка переменных окружения

### Обязательно добавьте в `.env` файл:

```env
# S3 Storage Configuration
S3_ACCESS_KEY=your_access_key_here
S3_SECRET_KEY=your_secret_key_here
S3_ENDPOINT_URL=https://s3.ru-7.storage.selcloud.ru
S3_BUCKET_NAME=anigo
S3_DOMEN_URL=https://40ae02b4-dcfc-4ffc-a604-1112971df1d8.selstorage.ru
```

⚠️ **ВАЖНО:** Замените `your_access_key_here` и `your_secret_key_here` на реальные значения!

## Новые файлы

### 1. `backend/src/utils/file_wrapper.py`
Обертка для работы с файлами при загрузке в S3.

### 2. `backend/src/utils/image_validator.py`
Утилита для валидации изображений с готовыми валидаторами:
- `AVATAR_VALIDATOR` - для аватаров (макс. 2 МБ, 2000x2000 px)
- `BACKGROUND_IMAGE_VALIDATOR` - для фоновых изображений (макс. 5 МБ, точно 1200x350 px)

## Миграция базы данных

Для применения индексов выполните:

```bash
cd backend
python -m alembic revision --autogenerate -m "add_indexes_to_user_profile_settings"
python -m alembic upgrade head
```

Или создайте миграцию вручную:

```sql
-- Добавление индексов
CREATE INDEX IF NOT EXISTS ix_user_profile_settings_user_id 
ON user_profile_settings(user_id);

CREATE INDEX IF NOT EXISTS ix_user_profile_settings_collector_badge_expires_at 
ON user_profile_settings(collector_badge_expires_at);
```

## Изменения в API

### `PATCH /user/background-image`
**Изменения:**
- ✅ Теперь требует премиум подписку (premium/admin/owner)
- ✅ Возвращает ошибку 403 для обычных пользователей

**Пример ответа для не-премиум пользователя:**
```json
{
  "detail": "Фоновое изображение доступно только для премиум пользователей"
}
```

## Использование новых утилит

### Пример использования ImageValidator:

```python
from src.utils.image_validator import AVATAR_VALIDATOR, BACKGROUND_IMAGE_VALIDATOR

# Для аватара
file_content = await AVATAR_VALIDATOR.validate(uploaded_file)

# Для фонового изображения
file_content = await BACKGROUND_IMAGE_VALIDATOR.validate(uploaded_file)
```

### Пример использования FileWrapper:

```python
from src.utils.file_wrapper import FileWrapper

file_wrapper = FileWrapper(
    file_content=bytes_data,
    filename="image.jpg",
    content_type="image/jpeg"
)

# Использование с S3
await s3_client.upload_user_photo(file_wrapper, user_id)
```

## Безопасность

### До улучшений ❌
```python
# Креденшалы в коде (ОПАСНО!)
s3_client = S3Client(
    access_key='287f46f37d90482980a2e453b185163f',
    secret_key='a16f368a0146495dbc86c259d9ef0ebf',
    ...
)
```

### После улучшений ✅
```python
# Креденшалы в .env файле (БЕЗОПАСНО!)
s3_client = S3Client(
    access_key=os.getenv('S3_ACCESS_KEY', ''),
    secret_key=os.getenv('S3_SECRET_KEY', ''),
    ...
)
```

## Проверка работоспособности

1. Убедитесь, что `.env` файл содержит все необходимые переменные
2. Перезапустите бэкенд
3. Попробуйте загрузить фоновое изображение (требуется премиум)
4. Проверьте логи на наличие ошибок

## Следующие шаги

1. ✅ Добавьте реальные S3 креденшалы в `.env`
2. ✅ Выполните миграцию для добавления индексов
3. ✅ Перезапустите приложение
4. ✅ Протестируйте загрузку изображений
5. ✅ Проверьте работу премиум-ограничений

## Контрольный список

- [ ] `.env` файл создан и заполнен
- [ ] Миграция выполнена
- [ ] Приложение перезапущено
- [ ] Загрузка аватара работает
- [ ] Загрузка фона работает только для премиум
- [ ] Старые изображения удаляются автоматически

## Поддержка

Если возникли проблемы:
1. Проверьте логи бэкенда
2. Убедитесь, что все переменные окружения установлены
3. Проверьте доступность S3 хранилища
4. Проверьте права доступа к S3 bucket
