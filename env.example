# ============================================
# AniGo - Пример конфигурации переменных окружения
# ============================================
# Скопируйте этот файл в .env и заполните реальными значениями
# НИКОГДА не коммитьте .env файл в git!
# 
# Команда для копирования:
# cp env.example .env

# ============================================
# БАЗА ДАННЫХ (PostgreSQL)
# ============================================
POSTGRES_USER=your_postgres_user
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=anigo
DB_HOST=db
DB_PORT=5432

# ============================================
# REDIS (для кэширования)
# ============================================
# Можно использовать либо REDIS_URL, либо отдельные переменные
REDIS_URL=redis://redis:6379/0
# Или отдельно:
# REDIS_HOST=redis
# REDIS_PORT=6379
# REDIS_DB=0
# REDIS_PASSWORD=  # Оставьте пустым, если пароль не используется

# ============================================
# JWT И БЕЗОПАСНОСТЬ
# ============================================
# ВАЖНО: Сгенерируйте безопасный SECRET_KEY для production!
# Можно использовать: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your_secret_key_here_minimum_32_characters
ALGORITHM=HS256
COOKIES_SESSION_ID_KEY=session_id

# ============================================
# SMTP (для отправки email)
# ============================================
# Настройки для Gmail:
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your_app_password_here  # Для Gmail нужен пароль приложения!
SMTP_FROM_EMAIL=your-email@gmail.com

# Или для reg.ru:
# SMTP_HOST=mail.hosting.reg.ru
# SMTP_PORT=587
# SMTP_USER=noreply@yourdomain.ru
# SMTP_PASSWORD=your_password
# SMTP_FROM_EMAIL=noreply@yourdomain.ru

# URL фронтенда для ссылок в письмах
FRONTEND_URL=https://yourdomain.com

# ============================================
# S3 ХРАНИЛИЩЕ (для загрузки аватарок и изображений)
# ============================================
S3_ACCESS_KEY=your_s3_access_key
S3_SECRET_KEY=your_s3_secret_key
S3_ENDPOINT_URL=https://s3.ru-7.storage.selcloud.ru
S3_BUCKET_NAME=anigo
S3_DOMEN_URL=https://your-bucket-id.selstorage.ru

# ============================================
# CORS (разрешенные домены)
# ============================================
# Укажите через запятую все домены, с которых разрешены запросы
# Пример: ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ============================================
# ПУТИ И ФАЙЛЫ
# ============================================
# Путь к директории с аватарками (для Docker обычно /app)
AVATARS_BASE_PATH=/app

# ============================================
# ОКРУЖЕНИЕ
# ============================================
# Установите production для production сервера
ENVIRONMENT=production

# ============================================
# FRONTEND (для разработки)
# ============================================
# VITE_API_URL используется только в development режиме
# В production фронтенд использует относительные пути через nginx
VITE_API_URL=http://localhost:8000
