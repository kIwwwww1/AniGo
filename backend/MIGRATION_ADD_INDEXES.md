# Миграция: Добавление индексов к user_profile_settings

## Описание

Эта миграция добавляет индексы к таблице `user_profile_settings` для улучшения производительности запросов.

## Добавленные индексы

1. **ix_user_profile_settings_user_id** - индекс на поле `user_id`
   - Ускоряет поиск настроек профиля по ID пользователя
   - Используется в функциях: `get_user_profile_settings`, `get_or_create_user_profile_settings`

2. **ix_user_profile_settings_collector_badge_expires_at** - индекс на поле `collector_badge_expires_at`
   - Ускоряет проверку активных бейджей "Коллекционер #1"
   - Используется в функциях: `get_or_create_current_cycle`, `format_profile_settings_data`

## Применение миграции

### Способ 1: Через Python скрипт (рекомендуется)

```bash
cd backend
python migrations/run_add_indexes_migration.py
```

### Способ 2: Через Docker (если используется Docker)

```bash
# Войти в контейнер
docker exec -it anigo_backend bash

# Запустить миграцию
cd /app
python migrations/run_add_indexes_migration.py
```

### Способ 3: Напрямую через psql

```bash
# Подключиться к базе данных
psql -h localhost -U your_user -d your_database

# Применить миграцию
\i backend/migrations/add_indexes_to_user_profile_settings.sql
```

## Проверка результата

После применения миграции вы должны увидеть:

```
✅ Миграция успешно применена!
📊 Созданные индексы:
  - ix_user_profile_settings_collector_badge_expires_at: CREATE INDEX ...
  - ix_user_profile_settings_user_id: CREATE INDEX ...
  - user_profile_settings_pkey: CREATE UNIQUE INDEX ...
  - user_profile_settings_user_id_key: CREATE UNIQUE INDEX ...
```

## Откат миграции

Если нужно откатить миграцию:

```sql
DROP INDEX IF EXISTS ix_user_profile_settings_user_id;
DROP INDEX IF EXISTS ix_user_profile_settings_collector_badge_expires_at;
```

## Влияние на производительность

### До миграции
- Поиск настроек профиля: ~10-50ms (для больших таблиц)
- Проверка активных бейджей: ~20-100ms (для больших таблиц)

### После миграции
- Поиск настроек профиля: ~1-5ms
- Проверка активных бейджей: ~1-5ms

## Безопасность

- ✅ Миграция безопасна и не удаляет данные
- ✅ Использует `CREATE INDEX IF NOT EXISTS` для идемпотентности
- ✅ Можно применять многократно без ошибок
- ✅ Не блокирует таблицу надолго (индексы создаются быстро)

## Требования

- PostgreSQL 10+
- Доступ к базе данных с правами на создание индексов
- Python 3.10+ (для скрипта миграции)
- asyncpg (должен быть установлен)

## Устранение неполадок

### Ошибка: "DATABASE_URL не установлен"
**Решение:** Убедитесь, что в `.env` файле есть `DATABASE_URL`

### Ошибка: "permission denied"
**Решение:** Запустите миграцию от пользователя с правами на создание индексов

### Ошибка: "relation does not exist"
**Решение:** Убедитесь, что таблица `user_profile_settings` существует

## Примечания

- Индексы создаются автоматически при запуске приложения, если используется SQLAlchemy
- Эта миграция нужна только если SQLAlchemy не создает индексы автоматически
- Рекомендуется применять в off-peak время для больших баз данных
