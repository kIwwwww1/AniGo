# Настройка SSL сертификата от reg.ru (DomainSSL)

У вас уже есть активный SSL сертификат от reg.ru до 09.08.2026. Вместо получения нового через Let's Encrypt, настроим использование существующего сертификата.

## 📋 Шаги настройки:

### 1. Скачайте файлы сертификата с reg.ru

В панели управления reg.ru найдите раздел "Файлы сертификата" и скачайте:

- **Сертификат** (обычно файл `.crt` или `.pem`)
- **Private Key** (файл `.key`)
- **Корневой сертификат** (опционально, файл `.ca-bundle` или `.chain.pem`)

Сохраните их с понятными именами:
- `yumivo.ru.crt` - основной сертификат
- `yumivo.ru.key` - приватный ключ
- `yumivo.ru.chain.pem` - цепочка сертификатов (если есть)

### 2. Создайте директорию для сертификатов

```bash
cd /Users/kiww1/AniGo
mkdir -p ssl/yumivo.ru
```

### 3. Разместите файлы сертификата

Скопируйте скачанные файлы в директорию:

```bash
# Скопируйте файлы сертификата
cp /path/to/downloaded/yumivo.ru.crt ssl/yumivo.ru/
cp /path/to/downloaded/yumivo.ru.key ssl/yumivo.ru/
cp /path/to/downloaded/yumivo.ru.chain.pem ssl/yumivo.ru/  # если есть
```

### 4. Обновите docker-compose.prod.yml

Добавьте монтирование директории с сертификатами в nginx контейнер.

### 5. Обновите nginx.conf

Раскомментируйте HTTPS секцию и укажите пути к файлам сертификата от reg.ru.

### 6. Перезапустите nginx

```bash
docker-compose -f docker-compose.prod.yml restart nginx
```

## 🔍 Проверка сертификата

После настройки проверьте:

```bash
# Проверка HTTPS
curl -I https://yumivo.ru

# Проверка информации о сертификате
openssl s_client -connect yumivo.ru:443 -servername yumivo.ru < /dev/null 2>/dev/null | openssl x509 -noout -dates
```

## ⚠️ Важно:

1. **Безопасность**: Файл `yumivo.ru.key` содержит приватный ключ - храните его в безопасности!
2. **Права доступа**: Установите правильные права на файлы:
   ```bash
   chmod 644 ssl/yumivo.ru/*.crt
   chmod 600 ssl/yumivo.ru/*.key
   ```
3. **Обновление**: Сертификат действителен до 09.08.2026. Не забудьте обновить его до истечения срока!

## 🔄 Автоматическое обновление (опционально)

Если хотите автоматизировать обновление сертификата от reg.ru, можно:
1. Использовать API reg.ru для автоматического получения нового сертификата
2. Или переключиться на Let's Encrypt для автоматического обновления
