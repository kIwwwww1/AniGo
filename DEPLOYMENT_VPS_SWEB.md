# Подробная инструкция по развертыванию AniGo на VPS (vps.sweb.ru + reg.ru)

Это максимально подробная инструкция для развертывания сайта на VPS сервере от vps.sweb.ru с доменом от reg.ru.

## 📋 Содержание

1. [Подготовка домена в reg.ru](#1-подготовка-домена-в-regru)
2. [Подключение к VPS серверу](#2-подключение-к-vps-серверу)
3. [Установка Docker и Docker Compose](#3-установка-docker-и-docker-compose)
4. [Загрузка проекта на сервер](#4-загрузка-проекта-на-сервер)
5. [Настройка переменных окружения](#5-настройка-переменных-окружения)
6. [Настройка DNS записей](#6-настройка-dns-записей)
7. [Настройка Nginx и SSL](#7-настройка-nginx-и-ssl)
8. [Запуск приложения](#8-запуск-приложения)
9. [Проверка работоспособности](#9-проверка-работоспособности)
10. [Настройка автоматического обновления SSL](#10-настройка-автоматического-обновления-ssl)

---

## 1. Подготовка домена в reg.ru

### Шаг 1.1: Вход в панель управления reg.ru

1. Откройте сайт [reg.ru](https://www.reg.ru)
2. Войдите в личный кабинет
3. Перейдите в раздел "Мои домены"
4. Найдите ваш домен и откройте его настройки

### Шаг 1.2: Получение IP адреса вашего VPS

**ВАЖНО:** Сначала нужно узнать IP адрес вашего VPS сервера.

1. Войдите в панель управления vps.sweb.ru
2. Найдите ваш VPS сервер
3. Скопируйте **внешний IP адрес** (обычно выглядит как `123.45.67.89`)
4. Запишите этот IP - он понадобится для настройки DNS

**Пример:** `185.123.45.67`

---

## 2. Подключение к VPS серверу

### Шаг 2.1: Получение данных для подключения

В панели vps.sweb.ru найдите:
- **IP адрес сервера** (например: `185.123.45.67`)
- **Логин** (обычно `root` или имя пользователя)
- **Пароль** или **SSH ключ**

### Шаг 2.2: Подключение через SSH

**На Windows:**
1. Установите [PuTTY](https://www.putty.org/) или используйте PowerShell
2. В PowerShell выполните:
   ```powershell
   ssh root@185.123.45.67
   ```
   (замените `185.123.45.67` на ваш IP)

**На Mac/Linux:**
```bash
ssh root@185.123.45.67
```

**На Windows 10/11 (PowerShell):**
```powershell
ssh root@185.123.45.67
```

При первом подключении подтвердите добавление сервера (введите `yes`).

### Шаг 2.3: Обновление системы

После подключения выполните:

```bash
# Обновляем список пакетов
apt update

# Обновляем систему
apt upgrade -y

# Устанавливаем необходимые утилиты
apt install -y curl wget git nano ufw
```

---

## 3. Установка Docker и Docker Compose

### Шаг 3.1: Установка Docker

Выполните команды по порядку:

```bash
# Удаляем старые версии Docker (если есть)
apt remove -y docker docker-engine docker.io containerd runc

# Устанавливаем зависимости
apt install -y ca-certificates curl gnupg lsb-release

# Добавляем официальный GPG ключ Docker
mkdir -p /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg

# Добавляем репозиторий Docker
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
  $(lsb_release -cs) stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null

# Обновляем список пакетов
apt update

# Устанавливаем Docker
apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

# Проверяем установку
docker --version
```

Должно вывести что-то вроде: `Docker version 24.x.x`

### Шаг 3.2: Установка Docker Compose (если не установился)

```bash
# Проверяем, установлен ли Docker Compose
docker compose version

# Если команда не работает, устанавливаем отдельно:
curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
chmod +x /usr/local/bin/docker-compose

# Проверяем
docker-compose --version
```

### Шаг 3.3: Настройка Docker (опционально, но рекомендуется)

```bash
# Добавляем текущего пользователя в группу docker (чтобы не использовать sudo)
usermod -aG docker $USER

# Настраиваем автозапуск Docker
systemctl enable docker
systemctl start docker

# Проверяем статус
systemctl status docker
```

**Примечание:** После добавления в группу docker нужно переподключиться к серверу.

---

## 4. Загрузка проекта на сервер

### Вариант A: Загрузка через Git (рекомендуется)

```bash
# Переходим в домашнюю директорию
cd ~

# Клонируем репозиторий (замените URL на ваш)
git clone https://github.com/yourusername/anigo.git
# ИЛИ если репозиторий приватный:
git clone https://yourusername:yourtoken@github.com/yourusername/anigo.git

# Переходим в директорию проекта
cd anigo
```

### Вариант B: Загрузка через SCP/SFTP

**На вашем локальном компьютере:**

```bash
# Создайте архив проекта (в директории проекта)
tar -czf anigo.tar.gz --exclude='node_modules' --exclude='.git' --exclude='__pycache__' .

# Загрузите на сервер
scp anigo.tar.gz root@185.123.45.67:/root/

# На сервере распакуйте
ssh root@185.123.45.67
cd /root
tar -xzf anigo.tar.gz -C anigo
cd anigo
```

### Вариант C: Загрузка через FTP/SFTP клиент

1. Установите FileZilla или WinSCP
2. Подключитесь к серверу по SFTP:
   - Хост: `185.123.45.67`
   - Порт: `22`
   - Протокол: `SFTP`
   - Логин: `root`
   - Пароль: ваш пароль
3. Загрузите все файлы проекта в `/root/anigo`

---

## 5. Настройка переменных окружения

### Шаг 5.1: Создание файла .env

```bash
# Переходим в директорию проекта
cd ~/anigo

# Копируем пример конфигурации
cp env.example .env

# Открываем файл для редактирования
nano .env
```

### Шаг 5.2: Заполнение переменных окружения

**ВАЖНО:** Замените все значения на реальные!

#### 5.2.1. База данных

```env
POSTGRES_USER=anigo_user
POSTGRES_PASSWORD=ВашНадежныйПароль123!@#
POSTGRES_DB=anigo
DB_HOST=db
DB_PORT=5432
```

**Совет:** Используйте генератор паролей или создайте сложный пароль:
```bash
# Генерация пароля (выполните на сервере)
openssl rand -base64 32
```

#### 5.2.2. JWT и безопасность

```env
# Генерируем SECRET_KEY (выполните на сервере):
python3 -c "import secrets; print(secrets.token_urlsafe(32))"

# Скопируйте сгенерированный ключ в .env:
SECRET_KEY=ваш_сгенерированный_ключ_минимум_32_символа
ALGORITHM=HS256
COOKIES_SESSION_ID_KEY=session_id
```

#### 5.2.3. SMTP настройки (для reg.ru)

Если у вас есть почта на reg.ru:

```env
SMTP_HOST=mail.hosting.reg.ru
SMTP_PORT=587
SMTP_USER=noreply@yourdomain.ru
SMTP_PASSWORD=ваш_пароль_от_почты
SMTP_FROM_EMAIL=noreply@yourdomain.ru
FRONTEND_URL=https://yourdomain.ru
```

**Или используйте Gmail:**

```env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=ваш_пароль_приложения_gmail
SMTP_FROM_EMAIL=your-email@gmail.com
FRONTEND_URL=https://yourdomain.ru
```

**Как получить пароль приложения Gmail:**
1. Войдите в аккаунт Google
2. Перейдите: https://myaccount.google.com/apppasswords
3. Создайте пароль приложения
4. Используйте его в `SMTP_PASSWORD`

#### 5.2.4. CORS (ваш домен)

```env
ALLOWED_ORIGINS=https://yourdomain.ru,https://www.yourdomain.ru
```

**Замените `yourdomain.ru` на ваш реальный домен!**

#### 5.2.5. Окружение

```env
ENVIRONMENT=production
AVATARS_BASE_PATH=/app
```

#### 5.2.6. Redis (опционально)

Если используете Redis:

```env
REDIS_URL=redis://redis:6379/0
```

#### 5.2.7. S3 (если используется)

```env
S3_ACCESS_KEY=ваш_ключ
S3_SECRET_KEY=ваш_секрет
S3_ENDPOINT_URL=https://s3.ru-7.storage.selcloud.ru
S3_BUCKET_NAME=anigo
S3_DOMEN_URL=https://your-bucket-id.selstorage.ru
```

### Шаг 5.3: Сохранение файла

В nano:
- Нажмите `Ctrl + O` для сохранения
- Нажмите `Enter` для подтверждения
- Нажмите `Ctrl + X` для выхода

---

## 6. Настройка DNS записей

### Шаг 6.1: Вход в панель управления доменом reg.ru

1. Откройте [reg.ru](https://www.reg.ru)
2. Войдите в личный кабинет
3. Перейдите в "Мои домены" → выберите ваш домен
4. Найдите раздел "DNS-серверы и зона"

### Шаг 6.2: Настройка DNS записей

**Вариант A: Использование DNS серверов reg.ru (рекомендуется)**

1. Убедитесь, что используются DNS серверы reg.ru:
   - `ns1.reg.ru`
   - `ns2.reg.ru`

2. Перейдите в раздел "Управление DNS-зоной"

3. Добавьте/измените следующие записи:

   **A-запись (основной домен):**
   ```
   Тип: A
   Имя: @ (или оставить пустым)
   Значение: 185.123.45.67 (ваш IP VPS)
   TTL: 3600
   ```

   **A-запись (www поддомен):**
   ```
   Тип: A
   Имя: www
   Значение: 185.123.45.67 (ваш IP VPS)
   TTL: 3600
   ```

4. Сохраните изменения

**Вариант B: Использование внешних DNS серверов**

Если вы используете Cloudflare или другие DNS:
- Добавьте A-запись `@` → `185.123.45.67`
- Добавьте A-запись `www` → `185.123.45.67`

### Шаг 6.3: Ожидание распространения DNS

DNS изменения могут распространяться от нескольких минут до 48 часов. Обычно это занимает 5-30 минут.

Проверить можно командой:
```bash
# На вашем компьютере
nslookup yourdomain.ru
# или
dig yourdomain.ru
```

Должен вернуться ваш IP адрес VPS.

---

## 7. Настройка Nginx и SSL

### Шаг 7.1: Подготовка Nginx конфигурации

```bash
# Переходим в директорию проекта
cd ~/anigo

# Копируем production конфигурацию
cp nginx/nginx.prod.conf nginx/nginx.conf

# Открываем для редактирования
nano nginx/nginx.conf
```

### Шаг 7.2: Замена домена в конфигурации

В файле `nginx/nginx.conf` замените **ВСЕ** вхождения `YOUR_DOMAIN` на ваш домен:

**Например, если ваш домен `example.ru`:**
- Замените `YOUR_DOMAIN` на `example.ru`
- Замените `www.YOUR_DOMAIN` на `www.example.ru`

**Используя sed (быстрый способ):**
```bash
# Замените yourdomain.ru на ваш реальный домен
sed -i 's/YOUR_DOMAIN/yourdomain.ru/g' nginx/nginx.conf
```

**Проверьте результат:**
```bash
grep -n "server_name" nginx/nginx.conf
```

Должно быть:
```
server_name yourdomain.ru www.yourdomain.ru;
```

### Шаг 7.3: Настройка firewall

```bash
# Разрешаем SSH (ВАЖНО! Сделайте это первым!)
ufw allow 22/tcp

# Разрешаем HTTP и HTTPS
ufw allow 80/tcp
ufw allow 443/tcp

# Включаем firewall
ufw enable

# Проверяем статус
ufw status
```

### Шаг 7.4: Запуск контейнеров для получения SSL

```bash
# Переходим в директорию проекта
cd ~/anigo

# Запускаем только nginx (для получения сертификата)
docker-compose -f docker-compose.prod.yml up -d nginx

# Ждем 10 секунд
sleep 10

# Проверяем, что nginx запустился
docker-compose -f docker-compose.prod.yml ps
```

### Шаг 7.5: Получение SSL сертификата от Let's Encrypt

```bash
# Получаем сертификат (замените email на ваш!)
docker-compose -f docker-compose.prod.yml run --rm certbot certonly \
  --webroot \
  --webroot-path=/var/www/certbot \
  --email your-email@example.com \
  --agree-tos \
  --no-eff-email \
  --non-interactive \
  --preferred-challenges http \
  -d yourdomain.ru \
  -d www.yourdomain.ru
```

**Если возникла ошибка:**
- Убедитесь, что DNS записи настроены и распространились
- Проверьте, что порт 80 открыт: `ufw status`
- Проверьте логи: `docker-compose -f docker-compose.prod.yml logs nginx`

### Шаг 7.6: Обновление путей к сертификатам в nginx.conf

После получения сертификата проверьте, что пути правильные:

```bash
# Проверяем наличие сертификатов
docker-compose -f docker-compose.prod.yml exec nginx ls -la /etc/letsencrypt/live/yourdomain.ru/
```

Должны быть файлы:
- `fullchain.pem`
- `privkey.pem`
- `chain.pem`

Если в nginx.conf указан правильный домен, пути должны быть корректными.

### Шаг 7.7: Перезапуск Nginx с SSL

```bash
# Перезапускаем nginx
docker-compose -f docker-compose.prod.yml restart nginx

# Проверяем конфигурацию
docker-compose -f docker-compose.prod.yml exec nginx nginx -t
```

Должно вывести: `nginx: configuration file /etc/nginx/conf.d/default.conf test is successful`

---

## 8. Запуск приложения

### Шаг 8.1: Запуск всех сервисов

```bash
# Переходим в директорию проекта
cd ~/anigo

# Останавливаем все контейнеры (если запущены)
docker-compose -f docker-compose.prod.yml down

# Собираем и запускаем все сервисы
docker-compose -f docker-compose.prod.yml up -d --build

# Ждем запуска (30-60 секунд)
sleep 30
```

### Шаг 8.2: Проверка статуса контейнеров

```bash
# Проверяем статус всех контейнеров
docker-compose -f docker-compose.prod.yml ps
```

Все контейнеры должны быть в статусе `Up`:
- `anigo-nginx-prod` - Up
- `anigo-backend-prod` - Up
- `anigo-frontend-prod` - Up
- `anigo-db-prod` - Up

### Шаг 8.3: Просмотр логов

```bash
# Все логи
docker-compose -f docker-compose.prod.yml logs -f

# Логи конкретного сервиса
docker-compose -f docker-compose.prod.yml logs -f backend
docker-compose -f docker-compose.prod.yml logs -f nginx
docker-compose -f docker-compose.prod.yml logs -f frontend
docker-compose -f docker-compose.prod.yml logs -f db
```

**Если видите ошибки:**
- Проверьте `.env` файл на правильность переменных
- Проверьте логи конкретного сервиса
- Убедитесь, что все порты свободны

---

## 9. Проверка работоспособности

### Шаг 9.1: Проверка сайта в браузере

1. Откройте браузер
2. Перейдите на `https://yourdomain.ru`
3. Должен открыться сайт с SSL сертификатом (зеленый замочек)

### Шаг 9.2: Проверка API

```bash
# Проверка health endpoint
curl https://yourdomain.ru/api/health

# Должен вернуть JSON:
# {"status":"healthy","database":"connected","redis":"connected"}
```

### Шаг 9.3: Проверка редиректа HTTP → HTTPS

```bash
# Проверяем редирект
curl -I http://yourdomain.ru

# Должна быть строка:
# HTTP/1.1 301 Moved Permanently
# Location: https://yourdomain.ru/...
```

### Шаг 9.4: Проверка SSL сертификата

Откройте сайт в браузере и проверьте:
- Зеленый замочек в адресной строке
- Сертификат выдан Let's Encrypt
- Срок действия: 90 дней

---

## 10. Настройка автоматического обновления SSL

SSL сертификаты Let's Encrypt действительны 90 дней. Нужно настроить автоматическое обновление.

### Шаг 10.1: Создание скрипта обновления

```bash
# Создаем директорию для скриптов
mkdir -p ~/scripts

# Создаем скрипт обновления
nano ~/scripts/renew-ssl.sh
```

Вставьте содержимое:

```bash
#!/bin/bash
cd /root/anigo
docker-compose -f docker-compose.prod.yml run --rm certbot renew
docker-compose -f docker-compose.prod.yml restart nginx
```

Сохраните (`Ctrl+O`, `Enter`, `Ctrl+X`)

```bash
# Делаем скрипт исполняемым
chmod +x ~/scripts/renew-ssl.sh
```

### Шаг 10.2: Настройка cron для автоматического обновления

```bash
# Открываем crontab
crontab -e

# Выберите редактор (обычно nano - нажмите 1)
```

Добавьте строку в конец файла:

```
0 3 * * * /root/scripts/renew-ssl.sh >> /var/log/ssl-renew.log 2>&1
```

Это будет обновлять сертификат каждый день в 3:00 ночи.

Сохраните и выйдите (`Ctrl+O`, `Enter`, `Ctrl+X`)

### Шаг 10.3: Проверка cron

```bash
# Просмотр настроенных задач
crontab -l

# Проверка логов cron
tail -f /var/log/cron.log
```

---

## 🔧 Полезные команды для управления

### Остановка приложения
```bash
cd ~/anigo
docker-compose -f docker-compose.prod.yml down
```

### Запуск приложения
```bash
cd ~/anigo
docker-compose -f docker-compose.prod.yml up -d
```

### Перезапуск после изменений
```bash
cd ~/anigo
docker-compose -f docker-compose.prod.yml restart
```

### Пересборка после изменений кода
```bash
cd ~/anigo
docker-compose -f docker-compose.prod.yml up -d --build
```

### Просмотр логов
```bash
# Все логи
docker-compose -f docker-compose.prod.yml logs -f

# Последние 100 строк
docker-compose -f docker-compose.prod.yml logs --tail=100

# Логи конкретного сервиса
docker-compose -f docker-compose.prod.yml logs -f backend
```

### Выполнение команд в контейнере
```bash
# Доступ к базе данных
docker-compose -f docker-compose.prod.yml exec db psql -U anigo_user -d anigo

# Выполнение миграций
docker-compose -f docker-compose.prod.yml exec backend python migrations/run_migration.py

# Bash в контейнере backend
docker-compose -f docker-compose.prod.yml exec backend bash
```

### Бэкап базы данных
```bash
# Создание бэкапа
docker-compose -f docker-compose.prod.yml exec db pg_dump -U anigo_user anigo > ~/backups/backup_$(date +%Y%m%d_%H%M%S).sql

# Восстановление из бэкапа
cat ~/backups/backup_20240101_120000.sql | docker-compose -f docker-compose.prod.yml exec -T db psql -U anigo_user -d anigo
```

---

## 🐛 Устранение проблем

### Проблема: Контейнеры не запускаются

```bash
# Проверьте логи
docker-compose -f docker-compose.prod.yml logs

# Проверьте статус
docker-compose -f docker-compose.prod.yml ps

# Пересоберите контейнеры
docker-compose -f docker-compose.prod.yml up -d --build --force-recreate
```

### Проблема: SSL сертификат не получен

```bash
# Проверьте DNS
nslookup yourdomain.ru

# Проверьте, что порт 80 открыт
ufw status

# Проверьте логи certbot
docker-compose -f docker-compose.prod.yml logs certbot

# Попробуйте получить сертификат вручную с подробным выводом
docker-compose -f docker-compose.prod.yml run --rm certbot certonly \
  --webroot \
  --webroot-path=/var/www/certbot \
  --email your-email@example.com \
  --agree-tos \
  --no-eff-email \
  -d yourdomain.ru \
  -d www.yourdomain.ru \
  --verbose
```

### Проблема: База данных не подключается

```bash
# Проверьте переменные окружения
docker-compose -f docker-compose.prod.yml exec backend env | grep POSTGRES

# Проверьте доступность БД
docker-compose -f docker-compose.prod.yml exec backend python -c "import asyncio; from src.db.database import engine; asyncio.run(engine.connect())"

# Проверьте логи БД
docker-compose -f docker-compose.prod.yml logs db
```

### Проблема: Сайт не открывается

```bash
# Проверьте, что контейнеры запущены
docker-compose -f docker-compose.prod.yml ps

# Проверьте логи nginx
docker-compose -f docker-compose.prod.yml logs nginx

# Проверьте конфигурацию nginx
docker-compose -f docker-compose.prod.yml exec nginx nginx -t

# Проверьте, что порты открыты
netstat -tulpn | grep -E ':(80|443)'
```

### Проблема: 502 Bad Gateway

```bash
# Проверьте, что backend запущен
docker-compose -f docker-compose.prod.yml ps backend

# Проверьте логи backend
docker-compose -f docker-compose.prod.yml logs backend

# Проверьте health endpoint
docker-compose -f docker-compose.prod.yml exec backend curl http://localhost:8000/health
```

---

## 📞 Получение помощи

Если возникли проблемы:

1. Проверьте логи: `docker-compose -f docker-compose.prod.yml logs`
2. Проверьте статус контейнеров: `docker-compose -f docker-compose.prod.yml ps`
3. Убедитесь, что все переменные в `.env` заполнены правильно
4. Проверьте DNS записи: `nslookup yourdomain.ru`
5. Проверьте firewall: `ufw status`

---

## ✅ Чеклист готовности к production

Перед тем как считать сайт готовым, убедитесь:

- [ ] Все переменные в `.env` заполнены реальными значениями
- [ ] `SECRET_KEY` сгенерирован и имеет минимум 32 символа
- [ ] `ALLOWED_ORIGINS` содержит ваш домен
- [ ] `FRONTEND_URL` указывает на production домен
- [ ] DNS записи настроены и распространились
- [ ] SSL сертификат получен и работает
- [ ] Сайт открывается по HTTPS
- [ ] Редирект HTTP → HTTPS работает
- [ ] API endpoint `/api/health` возвращает `{"status":"healthy"}`
- [ ] Firewall настроен (порты 22, 80, 443 открыты)
- [ ] Автоматическое обновление SSL настроено
- [ ] Бэкапы базы данных настроены (рекомендуется)

---

**Поздравляем! Ваш сайт должен быть доступен по адресу `https://yourdomain.ru`! 🎉**
