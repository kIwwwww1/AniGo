# Быстрый старт: Настройка SSL сертификата от reg.ru

## ✅ Что уже настроено:

1. ✅ Docker-compose настроен для монтирования сертификатов
2. ✅ Nginx конфигурация готова (HTTPS секция закомментирована до размещения файлов)
3. ✅ Директория `ssl/yumivo.ru/` создана
4. ✅ Nginx работает по HTTP (порт 80)

## 📋 Что нужно сделать:

### Шаг 1: Скачайте файлы сертификата с reg.ru

1. Войдите в панель управления reg.ru
2. Перейдите в раздел **"SSL сертификаты"**
3. Найдите ваш сертификат для **yumivo.ru**
4. В разделе **"Файлы сертификата"** скачайте:
   - **Сертификат** → сохраните как `yumivo.ru.crt`
   - **Private Key** → сохраните как `yumivo.ru.key`
   - **Корневой сертификат** → сохраните как `yumivo.ru.chain.pem` (если есть)

### Шаг 2: Разместите файлы в проекте

```bash
cd /Users/kiww1/AniGo/ssl/yumivo.ru

# Скопируйте скачанные файлы сюда:
# yumivo.ru.crt
# yumivo.ru.key
# yumivo.ru.chain.pem (если есть)
```

### Шаг 3: Установите права доступа

```bash
cd /Users/kiww1/AniGo/ssl/yumivo.ru
chmod 644 yumivo.ru.crt
chmod 600 yumivo.ru.key
chmod 644 yumivo.ru.chain.pem  # если есть
```

### Шаг 4: Раскомментируйте HTTPS секцию в nginx.conf

Откройте файл `nginx/nginx.conf` и:

1. Найдите строку `# server {` (около строки 129)
2. Раскомментируйте всю HTTPS секцию (уберите `#` в начале каждой строки)
3. Найдите строку `# }` в конце HTTPS секции и раскомментируйте её

**Или используйте команду:**
```bash
cd /Users/kiww1/AniGo
sed -i '' 's/^# server {/server {/' nginx/nginx.conf
sed -i '' 's/^#     listen/listen/' nginx/nginx.conf
sed -i '' 's/^#     http2/http2/' nginx/nginx.conf
sed -i '' 's/^#     server_name/server_name/' nginx/nginx.conf
sed -i '' 's/^#     # SSL/    # SSL/' nginx/nginx.conf
sed -i '' 's/^#     ssl_certificate/    ssl_certificate/' nginx/nginx.conf
sed -i '' 's/^#     ssl_certificate_key/    ssl_certificate_key/' nginx/nginx.conf
# ... и так далее для всех строк в HTTPS секции
```

**Лучше сделать вручную** - откройте файл и уберите `#` в начале строк HTTPS секции.

### Шаг 5: Включите редирект с HTTP на HTTPS

В файле `nginx/nginx.conf` найдите строки:
```nginx
    # Редирект на HTTPS (раскомментируйте после размещения файлов сертификата)
    # return 301 https://$host$request_uri;
```

Раскомментируйте строку с редиректом и удалите временный `location /` блок ниже.

### Шаг 6: Перезапустите nginx

```bash
cd /Users/kiww1/AniGo
docker-compose -f docker-compose.prod.yml restart nginx
```

### Шаг 7: Проверьте работу HTTPS

```bash
# Проверка HTTPS
curl -I https://yumivo.ru

# Проверка информации о сертификате
openssl s_client -connect yumivo.ru:443 -servername yumivo.ru < /dev/null 2>/dev/null | openssl x509 -noout -dates
```

## ⚠️ Важно:

- **Не коммитьте файлы сертификата в git!** Они уже добавлены в `.gitignore`
- Файл `yumivo.ru.key` содержит приватный ключ - храните его в безопасности
- Сертификат действителен до **09.08.2026** - не забудьте обновить его до истечения

## 🔍 Проверка логов:

Если что-то не работает, проверьте логи:

```bash
docker logs anigo-nginx-prod
docker exec anigo-nginx-prod nginx -t
```

## 📝 Подробная инструкция:

См. файл `ssl/yumivo.ru/README.md` для детальной информации.
