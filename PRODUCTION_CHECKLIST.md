# Чеклист подготовки к production

## ✅ Выполненные изменения

### 1. Конфигурация и переменные окружения
- ✅ Создан `env.example` с документацией всех переменных окружения
- ✅ Обновлен `.gitignore` для исключения `.env` файла
- ✅ Добавлена переменная `ENVIRONMENT` для управления режимом работы

### 2. Безопасность
- ✅ Отключена API документация (`/docs`, `/redoc`) в production режиме
- ✅ Исправлены хардкодные значения `localhost` в коде
- ✅ Настроен CORS через переменную `ALLOWED_ORIGINS`
- ✅ Добавлен `/health` endpoint для мониторинга

### 3. Docker и контейнеризация
- ✅ Оптимизирован `backend/Dockerfile` для production (добавлены workers)
- ✅ Добавлена переменная `ENVIRONMENT=production` в Dockerfile
- ✅ Исправлен healthcheck в `docker-compose.prod.yml` (использует `/health` вместо `/docs`)
- ✅ Добавлена переменная `ENVIRONMENT` в docker-compose.prod.yml

### 4. Nginx конфигурация
- ✅ Создан `nginx/nginx.prod.conf` с полной SSL конфигурацией
- ✅ Настроены security headers (HSTS, CSP, X-Frame-Options и др.)
- ✅ Настроен rate limiting для защиты от DDoS
- ✅ Настроен редирект HTTP → HTTPS

### 5. Документация
- ✅ Создан `DEPLOYMENT.md` с подробными инструкциями по развертыванию
- ✅ Создан `PRODUCTION_CHECKLIST.md` (этот файл)

## 📋 Перед деплоем проверьте:

### Обязательные настройки в `.env`:

1. **Безопасность:**
   ```env
   SECRET_KEY=<сгенерируйте_надежный_ключ>
   ENVIRONMENT=production
   ```

2. **Домены:**
   ```env
   FRONTEND_URL=https://yourdomain.com
   ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
   ```

3. **База данных:**
   ```env
   POSTGRES_USER=<надежный_пользователь>
   POSTGRES_PASSWORD=<надежный_пароль>
   ```

4. **SMTP:**
   ```env
   SMTP_USER=<ваш_email>
   SMTP_PASSWORD=<пароль_приложения>
   SMTP_FROM_EMAIL=<ваш_email>
   ```

### Настройка Nginx:

1. Скопируйте production конфигурацию:
   ```bash
   cp nginx/nginx.prod.conf nginx/nginx.conf
   ```

2. Замените `YOUR_DOMAIN` на ваш домен:
   ```bash
   sed -i 's/YOUR_DOMAIN/yourdomain.com/g' nginx/nginx.conf
   ```

3. Получите SSL сертификат (см. `DEPLOYMENT.md`)

### Проверка перед запуском:

- [ ] Все переменные в `.env` заполнены реальными значениями
- [ ] `SECRET_KEY` сгенерирован и имеет минимум 32 символа
- [ ] `ALLOWED_ORIGINS` содержит все ваши домены
- [ ] `FRONTEND_URL` указывает на production домен
- [ ] Nginx конфигурация обновлена с вашим доменом
- [ ] SSL сертификат получен и настроен
- [ ] Порты 80 и 443 открыты в firewall
- [ ] DNS записи настроены (A-запись на IP сервера)

### После деплоя проверьте:

- [ ] Сайт доступен по HTTPS: `https://yourdomain.com`
- [ ] Редирект HTTP → HTTPS работает
- [ ] API работает: `https://yourdomain.com/api/health`
- [ ] SSL сертификат валиден (проверьте в браузере)
- [ ] Логи не содержат ошибок:
  ```bash
  docker-compose -f docker-compose.prod.yml logs
  ```

## 🔒 Дополнительные рекомендации по безопасности:

1. **Firewall:**
   ```bash
   sudo ufw allow 22/tcp   # SSH
   sudo ufw allow 80/tcp   # HTTP
   sudo ufw allow 443/tcp  # HTTPS
   sudo ufw enable
   ```

2. **Регулярные обновления:**
   - Обновляйте систему: `sudo apt update && sudo apt upgrade`
   - Обновляйте Docker образы: `docker-compose pull`

3. **Мониторинг:**
   - Настройте мониторинг логов
   - Настройте алерты на ошибки
   - Регулярно проверяйте логи на подозрительную активность

4. **Бэкапы:**
   - Настройте автоматические бэкапы БД
   - Храните бэкапы в безопасном месте
   - Тестируйте восстановление из бэкапов

5. **SSL сертификаты:**
   - Настройте автоматическое обновление (cron)
   - Проверяйте срок действия сертификатов

## 📚 Дополнительная документация:

- Подробные инструкции по деплою: см. `DEPLOYMENT.md`
- Пример конфигурации: см. `env.example`
- Nginx конфигурация: см. `nginx/nginx.prod.conf`
